# Loopback-only endpoint for the local SWE-Gym/OpenHands-7B-Agent checkpoint.
# The evaluator reads this through OpenHands' standard LLM configuration path.
[llm.openhands7b]
model = "openai/openhands7b"
api_key = "local"
base_url = "http://127.0.0.1:8001/v1"
temperature = 0.0
log_completions = true
